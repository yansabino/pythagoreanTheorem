import React from 'react';
import {HeaderContainer, StyledTitle} from '../../style/Header'

const Header = () => {
  return (
    <HeaderContainer>
        <StyledTitle>Teorema de Pitagoras</StyledTitle>
    </HeaderContainer>
  );
}

export default Header;
